const chai = require('chai');
const expect = chai.expect;

describe('Test for issue 1', () => {
    it('Refactor getting all products to a controller file', async () => {
      expect(200).to.equal(200);
    });
});

describe('Test for issue 2', () => {
    it('Refactor getting all users to a controller file', () => {
      expect(200).to.equal(200);
    });
});
  
describe('Test for issue 3', () => {
    it('Refactor getting a user to a controller file', () => {
      expect(200).to.equal(200);
    });
});

describe('Test for issue 4', () => {
    it('Refactor deleting user to a controller file', () => {
      expect(200).to.equal(200);
    });
});

describe('Test for issue 5', () => {
    it('Refactor updating user to a controller file', () => {
      expect(200).to.equal(200);
    });
});

describe('Test for issue 6', () => {
    it('Refactor registering a new user to a controller file', () => {
      expect(200).to.equal(200);
    });
});

describe('Test for issue 7', () => {
    it('Remove any unnecessary requires and unused variables from routes.js', () => {
      expect(200).to.equal(200);
    });
});

describe('Test for issue 8', () => {
    it('Refactor getAllProductsFromCart-function to functional', () => {
      expect(200).to.equal(200);
    });
});

describe('Test for issue 9', () => {
    it('Refactor the userSchema to use values from SCHEMA_DEFAULTS', () => {
      expect(200).to.equal(200);
    });
});

describe('Test for issue 10', () => {
    it('Remove any unnecessary requires and unused variables from index.js', () => {
      expect(200).to.equal(200);
    });
});
